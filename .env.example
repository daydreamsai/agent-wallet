# ── SAW (Secure Agent Wallet) ─────────────────────────────────────────────
SAW_WALLET=main
SAW_CHAIN=evm
SAW_POLICY_TEMPLATE=conservative

# ── OpenClaw Gateway ──────────────────────────────────────────────────────
# Authentication token for the OpenClaw gateway. Always set to a strong value.
# Generate with: openssl rand -hex 32
OPENCLAW_GATEWAY_TOKEN=change-me-now
OPENCLAW_GATEWAY_BIND=lan
OPENCLAW_GATEWAY_PORT=18789

# ── Optional ──────────────────────────────────────────────────────────────
# GOG_KEYRING_PASSWORD=change-me-now   # OpenClaw Gmail OAuth keyring (only if using Gmail integration)
